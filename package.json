{
  "name": "@ledgermem/mastra",
  "version": "0.1.0",
  "description": "LedgerMem memory provider for Mastra agents.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": ["ledgermem", "mastra", "memory", "agent", "rag"],
  "author": "LedgerMem <founders@proofly.dev>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ledgermem/ledgermem-mastra.git"
  },
  "dependencies": {
    "@ledgermem/memory": "^0.1.0"
  },
  "peerDependencies": {
    "@mastra/core": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
